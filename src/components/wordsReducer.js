/***WORDSREDUCER.JS FILE***/

//Create the wordsReducer function
//The wordsReducer function receives the state and action parameters
const initialState = [
  
  ];
  //The wordsReducer function returns the state
  //The function uses a switch statement to determine which action to perform
  const wordsReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default wordsReducer;
  
  